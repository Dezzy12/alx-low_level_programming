#include "main.h"

/**
* _islower - A funtion that checks for lowercase characters
* @c: The character inputted
* Return: returns 1 if character is lowercase and 0 if otherwise
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
